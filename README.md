# 🌧️🔮 **RainPredictionKNN:**

**RainPredictionKNN** یک مدل ساده ماشین لرنینگ است که از الگوریتم **K-Nearest Neighbors (KNN)** برای پیش‌بینی بارش باران فردا استفاده می‌کند. این پروژه از داده‌های آب و هوایی استرالیا برای پیش‌بینی اینکه **آیا فردا باران می‌بارد یا نه** استفاده می‌کند.

## 🚀 **هدف پروژه**
هدف این پروژه پیش‌بینی "RainTomorrow" است که نشان می‌دهد آیا فردا باران می‌بارد یا خیر. داده‌ها شامل ویژگی‌هایی مانند:
- دما 🌡️
- رطوبت 💧
- فشار 📉
- و ویژگی‌های دیگر است.

---

## 📊 **ساختار پروژه**

### 1. **پیش‌پردازش داده‌ها** 🛠️
- مدیریت مقادیر گمشده
- کدگذاری ویژگی‌های کتگوریکال

### 2. **تقسیم داده‌ها به Train/Test** 📈
- داده‌ها به دو بخش آموزش و تست تقسیم می‌شوند تا مدل در بخش آموزش آموزش ببیند و بر روی داده‌های تست ارزیابی شود.

### 3. **آموزش مدل KNN با `k=5`** 🤖
- مدل **K-Nearest Neighbors (KNN)** برای پیش‌بینی با استفاده از ۵ نزدیک‌ترین همسایه آموزش داده می‌شود.

### 4. **ارزیابی مدل** 📊
- ارزیابی عملکرد مدل با استفاده از **Accuracy** و **F1 Score**.
- نمایش **Confusion Matrix**، **ROC Curve**، و **Class Distribution**.

---

## 🔧 **پیش‌نیازها**

برای اجرای این پروژه به Python 3.x و چند کتابخانه نیاز دارید:

```bash
pip install pandas numpy scikit-learn matplotlib seaborn
